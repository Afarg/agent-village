# 現状の課題と改善計画

ポートフォリオ・実運用の両観点で見つかった課題を、優先度順にまとめる。各項目は「課題 → 影響 → 修正法」の形式で記載する。

## 優先度: 高

### 1. デモ画像・GIFが無い

- **課題**: READMEに実際の動作画面（キャラクターが部屋を移動する様子、吹き出し表示、許可待ち演出）が1枚も無い。
- **影響**: ダッシュボード系のツールは「見た目」が最大のアピールポイントなのに、テキストだけでは全く伝わらない。閲覧者が数秒で離脱するリスクが最も高い項目。
- **修正法**: `npm run demo` でデモシードを起動し、キャラクターの移動・吹き出し表示・完了演出をGIFまたは短い動画として収録し、README冒頭に埋め込む。

### 2. コミット履歴が1コミットのみ

- **課題**: 開発の全過程を1コミットにまとめて公開したため、設計→実装→修正の反復が見えない。
- **影響**: 「一気に生成した」ように見え、実際の試行錯誤・設計判断のプロセスが伝わらない。
- **修正法**: 今後の機能追加・バグ修正は小さい単位でコミットし、理由をコミットメッセージに残す。既存の `docs/design/*.md` の更新履歴を参照しながら、主要な設計判断を `CHANGELOG.md` として遡って書き起こすのも一案。

### 3. LICENSEファイルが無い

- **課題**: リポジトリにライセンス表記が無い。
- **影響**: 第三者がコードを参照・再利用してよいか判断できない。
- **修正法**: MITライセンス等をリポジトリルートに追加する。

### 4. 自動テスト・CIが無い

- **課題**: `server/src/lib/*.js`（`layout.js` / `normalize.js` / `roomsStore.js` 等）にロジックがまとまっているが、検証するテストコードが存在しない。
- **影響**: リファクタリングや機能追加時にリグレッションを検知できない。
- **修正法**: Node標準の `node:test` または Vitest を導入し、純粋関数（`normalize.js`・`layout.js`）から着手する。GitHub Actionsで push・PR時に自動実行する。

## 優先度: 中

### 5. 英語READMEが薄い

- **課題**: 英語での説明が冒頭の1文しかない。
- **影響**: 海外の採用担当・エンジニアがリポジトリを評価しづらい。
- **修正法**: 主要セクション（特徴・使い方・図解のキャプション）を日英併記にするか、`README.en.md` を別途用意する。

### 6. hooks設定例に開発者のローカル絶対パスが残っていた

- **課題**: 元の `docs/manual-install-guide.md` の設定例が `D:/app/agents_app/...` という開発者個人のパスをそのまま使っていた（README側は `<agent-village-path>` に一般化済み）。
- **影響**: 他の利用者がそのままコピーすると動作しない。
- **修正法**: `docs/manual-install-guide.md` 側の例も `<agent-village-path>` のようなプレースホルダーに統一する。

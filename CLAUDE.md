# CLAUDE.md

`D:\app\agents_app` は3つのサブプロジェクトが連携する1本のパイプライン（① `img_to_pixcel_app` 写真→ピクセルキャラ変換 → ② `ani_convert_app` まばたき・歩行差分生成 → ③ `agents_app` 本体 ダッシュボード表示）。現状・直近の変更点は `docs/project-status.md` を参照。

## img_to_pixcel_app のピクセル変換検証ルール（必須）

`img_to_pixcel_app` のピクセル変換パイプライン（`app/pipeline.py` の `convert()`）に関するバグ修正・機能追加・動作確認を行う際は、**必ず以下の12パターン全てで実行して確認すること**。1パターン（元の報告ケース）だけを確認して「直った」と判断してはならない。

- `grid_size`: 32 / 64 / 128 の3種類
- `colors`: 8 / 10 の2種類
- `output_size`: 64 / 128 の2種類

（3 × 2 × 2 = 12パターン、入力ビュー構成に斜め・横向き・後ろ向きが絡む変更の場合はビュー構成の軸も併せて確認する）

**背景**: このプロジェクトでは過去に何度も、1つの設定でしか確認しなかった修正が別の設定（`grid_size` 違い等）では実は直っていなかった、というケースが起きている（`img_to_pixcel_app/docs/design/` の各更新履歴を参照）。`grid_size=16〜24` は腕・脚が潰れやすく非推奨のため対象外（UI上の案内と同じ基準）。

関連: `.claude/skills/fix-verify-loop/SKILL.md`（バグ修正時の原因調査→実装→検証ループの手順。本ルールはその「検証パターン設計の目安」を img_to_pixcel_app について具体的に固定したもの）。

## コンテキストログの保存（恒久ルール・2026-09-09確定・方針のみ）

セッションの経緯・文脈を後から人が読み返すための記録を、作業対象のサブプロジェクトの
`docs`フォルダ（例: `img_to_pixcel_app/docs/`）に、チャットリセット（/clear・/compact）前を
基本のタイミングとして保存する。ファイルはgitに上げない（対象サブプロジェクトが git
リポジトリの場合は`.gitignore`に追加すること）。**そのセッション（またはその区切り）で
Claudeがユーザーに最後に返した回答（テキスト全文。要約・言い換えではなく原文）も、
ログの末尾に残すこと**（2026-09-09追記・`D:\app\order_app\CLAUDE.md`と同じ内容）。

**現時点では方針のみ**（`D:\app\order_app`の`.claude/handoff/`のような実際の保存スキルは
未整備）。実際に必要になった時点で、同様の仕組み（保存先フォルダの決定・.gitignore設定・
保存スキルの作成）を整備する。

## エラーログの記録（恒久ルール・2026-09-10確定・方針のみ）

セッション中に発生したエラー・不具合と、その原因・改善方法を、**1つのファイルに
継続的に追記していく**（新しいエラーのたびに別ファイルを作らない）。上記
「コンテキストログの保存」と同じタイミングで、**発生したエラーがあれば**記録する。
保存先は作業対象のサブプロジェクトの`docs`フォルダ（例:
`img_to_pixcel_app/docs/error_log.md`）。gitへの扱い（上げる/上げない）は
サブプロジェクトの性質に応じて判断してよい（`D:\app\order_app\CLAUDE.md`では
「通常のドキュメントとしてコミットする」方針にしている＝個人情報を含まない
技術的な知見のため）。内容は症状・原因・対処・教訓を簡潔に。

**現時点では方針のみ**。実際の運用（ファイルの新設等）は必要になった時点で行う。

## 専門用語の説明（恒久ルール・2026-09-11確定）

回答内でエンジニアリング系・ビジネス系の専門用語（高校レベルを超えるもの）を使った場合、
**回答の末尾に用語説明の項目を追加する**こと。高校以下で習う一般的な言葉はユーザーが
理解済みのため説明不要。

- 判断基準: 「高校生が読んで意味が分かるか」。分かるなら対象外、分からなそうなら対象。
  （例: API・CI/CD・リグレッション・オンボーディング・KPI・ROI のような業界・技術特有の
  略語や専門用語が対象。「関数」「変数」程度の一般化した基礎語は対象外でよい。）
- 形式: 回答の最後に見出し（例:「## 用語説明」）を立て、その回答内で実際に使った対象語を
  箇条書きで簡潔に説明する。対象語を使っていない回答では見出し自体を付けない。
